import { hsl } from "@/lib/utils"

// Cores principais do tema
export const colors = [
  hsl("primary"), // Azul primário
  hsl("secondary"), // Roxo secundário
  hsl("success"), // Verde
  hsl("warning"), // Amarelo
  hsl("destructive"), // Vermelho
  hsl("info"), // Azul claro
]
