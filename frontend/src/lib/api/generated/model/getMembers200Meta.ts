/**
 * type AxiosResponse<T> = T
 */

export type GetMembers200Meta = {
  hasNextPage: boolean;
  hasPreviousPage: boolean;
  limit: number;
  page: number;
  totalItems: number;
  totalPages: number;
};
